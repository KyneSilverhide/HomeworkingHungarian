public class Config {

    public static final int MAX_NUMBER_OFF_SIMULTANEOUS_HOMEWORKING = 2;
    public static final int NUMBER_OF_HOMEWORKING_DAYS_PER_EMPLOYEE = 2;

    // Costs : algorithm will try to minimize cost, so homeworking should be lower to have priority
    public static final double HOME_WORKING_COST = 0.0;
    public static final double WORKING_DAYS_COST = 2.0;
}
